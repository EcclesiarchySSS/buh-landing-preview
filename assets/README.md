# Изображения и шрифты сайта

## Шрифты

`fonts/` — self-hosted variable-шрифты, подключены в `css/styles.css` через `@font-face`:

- `golos-text-*.woff2` — Golos Text 400–800, основной текст;
- `jetbrains-mono-*.woff2` — JetBrains Mono 400–700, служебные подписи «ведомости».

Подмножества: кириллица + латиница. Хранятся локально намеренно: без обращений
к серверам Google Fonts (152-ФЗ) и быстрее. Менять не нужно.

## Фото

| Файл | Где | Статус |
|---|---|---|
| `img/auditor.jpg` (720×900) + `img/auditor-400.jpg` | hero, портрет 4:5 | ✅ селфи от 2026-09-10, кадр 01; перекадрирован выше очков на шнурке |
| `img/diplom-1…6.jpg` | «Почему нам доверяют» → приложения П-1…П-6 | ✅ сканы от 2026-09-15 |

Фото офиса/команды и других фото **не будет** (сказала заказчица).

Документы: П-1 диплом РГУ (экономист, 2005) · П-2 диплом к.э.н. (2010) ·
П-3…П-5 переподготовка АНО «НИИДПО» 2025 (налоговый консультант, внутренний аудитор,
экономист-аналитик) · П-6 удостоверение о ПК (аналитик данных, 2025).
Исходники — в `/дипломы` в корне репо. Приложение к диплому РГУ не публикуем:
там дата рождения. Нострификацию (Черногория) на сайт не ставили.

## Как добавить документ

1. Повернуть в читаемую ориентацию, обрезать поля скана, длинная сторона 1200 px,
   JPEG q≈82 (в `backend/.venv` есть Pillow).
2. Положить в `img/diplom-N.jpg`, в `index.html` (блок `.docs`) скопировать `<li>`
   и поправить `src`/`href`, `width`/`height`, `alt` и подпись П-N.
   Скан встаёт целиком (`object-fit: contain`), клик открывает оригинал.
